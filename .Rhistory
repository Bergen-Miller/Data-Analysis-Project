library(tidyverse)
library(skimr)
library(summarytools)
load('Temp Data/temp_countyIncomePanel.RData')
load('Temp Data/temp_countyImmigrationData.RData')
setwd('C:/Users/bergl/OneDrive/School Spring 2026/ECNS 560/Data-Analysis-Project')
load('Temp Data/temp_countyIncomePanel.RData')
load('Temp Data/temp_countyImmigrationData.RData')
df = inflows[[1]]
df = inflows[[1]] |> mutate(across(c(y1_statefips:y2_countyfips), as.numeric())) |>
mutate(y1_fips = 1000*y1_statefips + y1_countyfips,
y2_fips = 1000*y2_statefips + y2_countyfips)
df = inflows[[1]] |> mutate(across(c(y1_statefips:y2_countyfips), as.numeric)) |>
mutate(y1_fips = 1000*y1_statefips + y1_countyfips,
y2_fips = 1000*y2_statefips + y2_countyfips)
df = inflows[[1]] |> mutate(across(c(y1_statefips:y2_countyfips), as.numeric)) |>
mutate(y1_fips = 1000*y1_statefips + y1_countyfips,
y2_fips = 1000*y2_statefips + y2_countyfips) |>
select(-c(y1_statefips:y2_countyfips)) |>
relocate(c(y1_fips, y2_fips))
reworkCols = function(df){ # df is the tibble object given to the function
newDf = df |> mutate(across(c(y1_statefips:y2_countyfips), as.numeric), # coerce fips codes
y1_fips = 1000*y1_statefips + y1_countyfips, # define new fips columns
y2_fips = 1000*y2_statefips + y2_countyfips) |>
select(-c(y1_statefips:y2_countyfips)) |> # remove redundant columns
relocate(c(y1_fips, y2_fips)) # move county fips identifier codes to front
}
library(future.apply)
plan(multisession, workers = parallel::detectCores() - 1)
newList <- future_lapply(inflows, reworkCols)
warnings()
View(newList)
View(newList)
# rename tibbles in the list by their starting year:
names(newList) = seq(2011, 2021)
# Combine immigration data from list into single tibble
inflows_combined = imap(inflows, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
pivot_wider(
id_cols = c(y1_fips, y2_fips),
names_from = year,
values_from = c(n1, n2, agi),
names_glue = "{.value}_{year}")
# rename tibbles in the list by their starting year:
names(newList) = as.character(seq(2011, 2021))
# Combine immigration data from list into single tibble
inflows_combined = imap(inflows, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
pivot_wider(
id_cols = c(y1_fips, y2_fips),
names_from = year,
values_from = c(n1, n2, agi),
names_glue = "{.value}_{year}")
rlang::last_trace()
# rename tibbles in the list by their starting year:
names(newList) = 2011:2021
# Combine immigration data from list into single tibble
inflows_combined = imap(inflows, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
pivot_wider(
id_cols = c(y1_fips, y2_fips),
names_from = year,
values_from = c(n1, n2, agi),
names_glue = "{.value}_{year}")
# Combine immigration data from list into single tibble
inflows_combined = imap(inflows, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
pivot_wider(
id_cols = c(y1_fips, y2_fips, y2_state, y2_countyname),
names_from = year,
values_from = c(n1, n2, agi),
names_glue = "{.value}_{year}")
imap(inflows, ~ .x |> mutate(year=.y))
rm(df)
# Rename and redefine columns in data.
reworkCols = function(df){ # df is the tibble object given to the function
newDf = df |> mutate(across(c(y1_statefips:y2_countyfips), as.numeric), # coerce fips codes
y1_fips = 1000*y1_statefips + y1_countyfips, # define new fips columns
y2_fips = 1000*y2_statefips + y2_countyfips) |>
select(-c(y1_statefips:y2_countyfips)) |> # remove redundant columns
relocate(c(y1_fips, y2_fips)) # move county fips identifier codes to front
}
plan(multisession, workers = parallel::detectCores() - 1)
plan(multisession, workers = parallel::detectCores() - 1)
newList = future_lapply(inflows, reworkCols)
rm(newList)
tictoc::tic()
plan(multisession, workers = parallel::detectCores() - 1)
newList = future_lapply(inflows, reworkCols)
tictoc::toc()
rm(newList)
tictoc::tic()
newList=list()
for(i in 1:length(inflows)){
newList[[i]] = reworkCols(inflows[[i]])
}
tictoc::toc()
View(newList)
# rename tibbles in the list by their starting year:
names(newList) = 2011:2021
# Combine immigration data from list into single tibble
inflows_combined = imap(newList, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
pivot_wider(
id_cols = c(y1_fips, y2_fips, y2_state, y2_countyname),
names_from = year,
values_from = c(n1, n2, agi),
names_glue = "{.value}_{year}")
inflows_combined |> summarize(n=n(), .by=c(y1_fips, y2_fips, y2_state, y2_countyname, year)) |> filter(n>1)
# Combine immigration data from list into single tibble
inflows_combined = imap(newList, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year)
inflows_combined |> summarize(n=n(), .by=c(y1_fips, y2_fips, y2_state, y2_countyname, year)) |> filter(n>1)
inflows_combined |> summarize(n=n(), .by=c(y1_fips, y2_fips, y2_state, y2_countyname, year)) |> filter(n>1) |> view()
inflows_combined |> group_by(y1_fips, y2_fips, y2_state, y2_countyname, year) |> summarize(n=n()) |> filter(n>1) |> view()
inflow_combined |> group_by(c(y1_fips, y2_fips, year)) |> summarize(n=n()) |> filter(n>1) |> view()
inflows_combined |> group_by(c(y1_fips, y2_fips, year)) |> summarize(n=n()) |> filter(n>1) |> view()
inflows_combined |> group_by(y1_fips, y2_fips, year) |> summarize(n=n()) |> filter(n>1) |> view()
inflows_combined |> group_by(y1_fips, y2_fips, year) |> summarize(num=n()) |> filter(num>1) |> view()
inflows_combined |> group_by(y1_fips, y2_fips, year) |> summarize(num=n()) |> filter(num>1) |> View()
dups = inflows_combined |> group_by(y1_fips, y2_fips, year) |> summarize(num=n()) |>
filter(num>1)
inflows_combined |> filter(y1_fips==1003 & y2_fips==28047 & year==2013) |> View()
unique(dups$year)
dups |> group_by(year) |> summarize(num=n()) |> View()
# make sure combinations of y1_fips, y2_fips, year are unique
stopifnot((inflows_combined |> group_by(y1_fips, y2_fips, year) |>
count() |> filter(n>1))>0)
inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1) |> View()
# make sure combinations of y1_fips, y2_fips, year are unique
stopifnot((inflows_combined |> group_by(y1_fips, y2_fips, year) |>
count() |> filter(n>1))==0)
dups = inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1)
# Combine immigration data from list into single tibble
inflows_combined = imap(newList, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
mutate(key = 1:nrow(inflows_combined))
dups = inflows_combined |> group_by(key) |> count() |> filter(n>1)
dups = inflows_combined |> group_by(y1_fips, y2_fips, year, .keep='key') |> count() |> filter(n>1)
View(dups)
dups = inflows_combined |> group_by(y1_fips, y2_fips, year, .keep=key) |> count() |> filter(n>1)
dups = inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1)
View(dups)
inflows_combined |> filter(c(y1_fips, y2_fips, year) %in% dups) |> View()
# See if duplicate rows are exactly identical
inflows_combined |> semi_join(dups, by = c("y1_fips", "y2_fips", "year")) |> View()
inflows_combined |> filter(paste(y1_fips, y2_fips, year) %in% paste(dups$y1_fips, dups$y2_fips, dups$year)) |> View()
# See if duplicate rows are exactly identical
inflows_combined |> filter(c(y1_fips, y2_fips, year) %in% c(dups$y1_fips, dups$y2_fips, dups$year)) |> View()
# Combine immigration data from list into single tibble
inflows_combined = imap(newList, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
relocate(y1_fips, y2_fips, year)
# make sure combinations of y1_fips, y2_fips, year are unique
stopifnot((inflows_combined |> group_by(y1_fips, y2_fips, year) |>
count() |> filter(n>1))==0)
dups = inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1)
# See if duplicate rows are exactly identical
inflows_combined |> filter(paste(y1_fips, y2_fips, year) %in% paste(dups$y1_fips, dups$y2_fips, dups$year)) |> View()
# Remove duplicates: It will work to keep the instance of duplicate (y1_fips, y2_fips, year) with the smaller key
newInflows = inflows_combined |> group_by(y1_fips, y2_fips, year) |>
mutate(lowerKey=min(key)) |>
ungroup() |>
filter(key==lowerKey)
rm(inflows_combined)
relocate(key, y1_fips, y2_fips, year)
# Combine immigration data from list into single tibble
inflows_combined = imap(newList, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
mutate(key=1:length(inflows_combined)) |>
relocate(key, y1_fips, y2_fips, year)
# Combine immigration data from list into single tibble
inflows_combined = imap(newList, ~ .x |> mutate(year = .y)) |>
bind_rows() |>
select(y1_fips, y2_fips, y2_state, y2_countyname, n1, n2, agi, year) |>
relocate(y1_fips, y2_fips, year)
# Add key column
inflows_combined = inflows_combined |> mutate(key=1:length(inflows_combined)) |>
relocate(key)
# Add key column
inflows_combined = inflows_combined |> mutate(key=1:nrow(inflows_combined)) |>
relocate(key)
# make sure combinations of y1_fips, y2_fips, year are unique
stopifnot((inflows_combined |> group_by(y1_fips, y2_fips, year) |>
count() |> filter(n>1))==0)
dups = inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1)
# make sure combinations of y1_fips, y2_fips, year are unique
# If the following line prints 'FALSE', there are duplicates
(inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1))==0
dups = inflows_combined |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1)
# Remove duplicates: It will work to keep the instance of duplicate (y1_fips, y2_fips, year) with the smaller key
newInflows = inflows_combined |> group_by(y1_fips, y2_fips, year) |>
mutate(lowerKey=min(key)) |>
ungroup() |>
filter(key==lowerKey)
nrow(inflows_combined)-nrow(dups)
newInflows
newInflows |> view()
newInflows |> View()
newInflows |> group_by(y1_fips, y2_fips, year) |> count() |> filter(n>1) |> View()
length(unique(newInflows$key))
nrow(newInflows)
length(newInflows$key) - length(unique(newInflows$key))
migration = newInflows |> select(-lowerKey)
save(migration, file='Temp Data/temp_mergedImmigrationData.RData')
